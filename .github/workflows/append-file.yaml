name: POC_append_file
on: 
  push:
    branches: [ action-ci ]
    paths:
      - .github/workflows/append-file.yaml

jobs:
  append-file:
    runs-on: ubuntu-latest
    steps:
      - name: create new file
        run: |
            echo "Run ID: ${{ github.run_id }}"
            echo "Run ID Num: ${{ github.run_number }}"
            echo "Run ID Attempt: ${{ github.run_attempt }}"

